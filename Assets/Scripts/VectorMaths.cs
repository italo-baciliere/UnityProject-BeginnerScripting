using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VectorMaths : MonoBehaviour
{

    /* A primer on Vector maths - as well information on the 
        Dot and Cross products. */

    /*
     * In game development we use vectors to define meshes, directions 
     *      and all manner of other calculations, wich makes them essential
     *      to understand.
     *      
     * A vetor is a line drawn between two points,
     *      vectors also have a length know as their magnitude.
     */

    // Start is called before the first frame update
    void Start()
    {
         
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
